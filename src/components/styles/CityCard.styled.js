import styled from "styled-components";

export const StyledCityCard = styled.section`
  //   border: solid 30px red;

  .image-label {
    position: relative;
  }

  .city-card-container {
    position: relative;
    margin: 0.5rem;
    margin-bottom: 5rem;
  }

  img {
    border-radius: 1rem;
    width: 100%;
    position: absolute;
  }
`;
